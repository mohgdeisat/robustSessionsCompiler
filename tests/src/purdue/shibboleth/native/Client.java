import java.net.*;

public class Client implements Runnable{

    public void run() {
	Socket idp = new Socket("mc05.cs.purdue.edu", 7000);
	Socket idp = new Socket("sslab05.cs.purdue.edu", 7001);
	int count = 1000;
	int i = count;
	while (i-- > 0) {
	    
	}
    }

}